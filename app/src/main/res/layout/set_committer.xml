<RelativeLayout
	xmlns:android="http://schemas.android.com/apk/res/android"
	xmlns:app="http://schemas.android.com/apk/res-auto"
	android:layout_width="match_parent"
	android:layout_height="match_parent"
	android:background="?android:attr/windowBackground">

	<include
		layout="@layout/toolbar"/>

	<com.rengwuxian.materialedittext.MaterialEditText
		android:id="@+id/committer_name"
		android:layout_width="match_parent"
		android:layout_height="wrap_content"
		android:layout_below="@+id/toolbar_wrapper"
		android:layout_margin="8dp"
		android:singleLine="true"
		android:hint="@string/committer_name"
		android:inputType="textPersonName"
		app:met_textColor="@color/white"
		app:met_textColorHint="@color/light_grey"
		app:met_floatingLabel="highlight"/>

	<com.rengwuxian.materialedittext.MaterialEditText
		android:id="@+id/committer_email"
		android:layout_width="match_parent"
		android:layout_height="wrap_content"
		android:layout_below="@id/committer_name"
		android:layout_margin="8dp"
		android:singleLine="true"
		android:hint="@string/committer_email"
		android:inputType="textEmailAddress"
		app:met_textColor="@color/white"
		app:met_textColorHint="@color/light_grey"
		app:met_floatingLabel="highlight"/>

</RelativeLayout>
